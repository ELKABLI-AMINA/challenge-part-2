#include <stdio.h>

int main() {
	int a;
	
        printf("veuillez entrer la valeur de a");
        scanf("%d",&a);
        if (a<0)
        printf("le nombre est negatif");
        else if (a>0)
        printf("le nombre est positif");
        else
        printf("le nombre est agal a zero");
        return 0;
        }
	

