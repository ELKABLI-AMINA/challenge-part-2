#include <stdio.h>

int main() {
	
        int n ;
        printf("veuillez entrer la valeur de n");
        scanf("%d",&n);
        if(n%2==0)
        printf("%d est paire:",n);
        else
        printf("%d est impaire",n);

	return 0;
}
